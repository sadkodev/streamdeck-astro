---
import { useUserAgent } from 'astro-useragent'

import Layout from '../layouts/Layout.astro'

const uaString = Astro.request.headers.get('user-agent')

const { source, isTablet } = useUserAgent(uaString)

console.log(source)
---

<Layout>
  <!-- Modal toggle -->
  {!isTablet ? 'Not Support' : ''}
</Layout>

<script></script>
